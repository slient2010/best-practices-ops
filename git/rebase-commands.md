# how to rebase and merge codes

The commands show how to rebase and merge codes, hope them can help you.

```shell
git rebase develop
git rebase --continue
git checkout --ours packages/projects/api
git add *
git status
git rebase --continue
```
